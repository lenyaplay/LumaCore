# native/third_party/ffmpeg/

Placeholder — becomes a git submodule pinned to a specific FFmpeg release tag
in Этап 3 (ARCHITECTURE.md §4: `--disable-everything`, no libx264/GPL,
hardware encoders only — mediacodec/videotoolbox/mediafoundation).

Not added yet: picking the pinned tag and vendoring the submodule is real
Этап 3 work, not part of the repository skeleton.
